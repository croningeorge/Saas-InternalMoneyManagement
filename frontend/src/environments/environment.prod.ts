// used when we run `ng serve --environment prod` or `ng build --environment prod`
export const environment = {
  production: true,
  // URL of live API
  apiUrl: "http://api-megaapp.eraminfotech.in/api"
};
